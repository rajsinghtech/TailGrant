---
apiVersion: v1
kind: Service
metadata:
  name: tailgrant
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: tailgrant-server
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
